# 船舶监控大屏开发文档

## 1. 项目简介

`marine-dashboard` 是一个基于 React 的船舶监控可视化大屏项目，面向船舶驾驶台、轮机监控和综合运行状态展示场景。项目整体风格偏“工业控制台 / 船舶中控台”，通过 3D 模型、实时数据面板、图表、地图和告警系统来模拟完整的船舶运行监测体验。

本项目支持：
- 主页综合仪表盘
- 主机监控页
- 发动机系统页
- 告警页
- 趋势分析页
- 航海地图页
- 配置页面
- 中英文双语切换
- 主题切换与全屏显示
- 3D 船体与发动机模型展示
- 模拟实时数据刷新

## 2. 技术栈

### 核心框架
- React 19
- React Scripts 5
- JavaScript / JSX

### UI 与动画
- Tailwind CSS
- Framer Motion
- Material Symbols 图标

### 3D 可视化
- `three`
- `@react-three/fiber`
- `@react-three/drei`

### 地图展示
- `leaflet`
- `react-leaflet`

### 测试
- `@testing-library/react`
- `@testing-library/jest-dom`
- `@testing-library/user-event`

## 3. 项目启动与构建

### 开发启动
```bash
npm install
npm start
```

### 生产构建
```bash
npm run build
```

### 测试
```bash
npm test
```

## 4. 目录结构说明

当前项目的核心代码主要位于 `src/` 目录：

- `src/App.js`：应用入口，挂载主仪表盘
- `src/Dashboard.jsx`：主页面总容器，包含顶部导航、主视图切换、弹窗、底部栏等
- `src/MainEngine.jsx`：主机监控页面
- `src/EngineSystems.jsx`：发动机系统页面
- `src/NauticalCharts.jsx`：航海地图页面
- `src/Alarms.jsx`：告警页面
- `src/trend.jsx`：趋势分析页面
- `src/ConfigPage.jsx`：配置页面
- `src/YachtModel.jsx`：3D 游艇 / 发动机 / 引擎系统模型封装
- `src/hooks/useRealTimeData.js`：实时模拟数据
- `src/hooks/useFullscreen.js`：全屏控制
- `src/hooks/useLanguage.jsx`：国际化文本与语言上下文

## 5. 应用入口与整体结构

### `App.js`

应用入口非常简洁，直接渲染 `Dashboard`：

```jsx
import React from 'react';
import Dashboard from './Dashboard';

function App() {
  return (
    <div className="App h-[100dvh] overflow-hidden">
      <Dashboard />
    </div>
  );
}

export default App;
```

### `Dashboard.jsx`

`Dashboard` 是整个系统的主容器，内部使用 `LanguageProvider` 包裹页面，负责：
- 语言状态管理
- 主题模式切换
- 全屏控制
- 搜索弹窗、设置弹窗、账户弹窗
- 主导航按钮切换
- 主页 / 主机 / 告警 / 趋势 / 海图 / 配置页面切换

主页面默认展示综合仪表盘，并提供底部状态栏和右侧实时数据展示。

## 6. 页面功能说明

### 6.1 综合仪表盘

主页是船舶运行的总览视图，主要分为三栏：

#### 左侧
- CCTV 预览窗口
- 系统健康状态
- 航行统计

#### 中间
- 3D 船体模型
- 旋转控制
- 自动旋转开关
- 当前配置标题信息

#### 右侧
- 实时坐标
- 风速与风向
- 吃水数据
- 航速与航向
- 当前活动告警

顶部还提供：
- 全屏按钮
- 搜索按钮
- 设置按钮
- 账户按钮

### 6.2 主机监控页 `MainEngine.jsx`

主机监控页用于展示发动机运行状态，包含：
- 4 台发动机选择
- 自动旋转开关
- 中央 3D 发动机模型
- RPM、排温、油压、涡轮转速标签
- 故障高亮告警叠加
- 右侧状态卡片
- 底部 5 个状态指标卡

该页面会结合实时告警数据判断是否显示故障高亮。

### 6.3 发动机系统页 `EngineSystems.jsx`

该页面更偏向系统拆解与部件交互，主要包括：
- 4 台柴油机切换
- 左侧系统选择菜单
- 中央系统模型图
- 热点点击查看系统参数
- 右侧状态摘要卡
- 底部动态指标条

点击热点后，会弹出参数卡，展示润滑、冷却、燃油、进气等系统数据。

### 6.4 航海地图页 `NauticalCharts.jsx`

该页面基于 Leaflet 实现航海地图场景，内容包括：
- 船舶列表
- 地图标记点
- 航线轨迹
- 船舶详情弹窗
- 实时数据侧边栏
- 告警卡片
- 紧急停车按钮

主要特点：
- 点击船舶可切换地图视角
- 支持中英文状态显示
- 地图会自动 fit bounds

### 6.5 告警页 `Alarms.jsx`

告警页用于集中查看与处理告警，包含：
- 活动告警 / 历史告警切换
- 优先级筛选
- 搜索过滤
- 一键确认告警
- 右侧诊断总结面板
- 设备状态概览
- 底部状态卡片

告警数据来自实时模拟数据钩子 `useAlarmsData`。

### 6.6 趋势页 `trend.jsx`

趋势页用于展示发动机历史趋势与曲线分析，包括：
- 曲线开关
- 燃油消耗曲线
- 实际功率曲线
- 转速曲线
- 排温曲线
- 气缸温度柱状图
- 右侧引擎 3D 模型
- AI 建议面板占位

该页面会根据实时引擎数据生成趋势图和气缸温差分析。

### 6.7 配置页 `ConfigPage.jsx`

配置页用于系统参数设置，包含：
- 显示设置
- 全屏模式开关
- 日间 / 夜间 / 跟随系统主题
- 中英文语言切换
- 本地 / 云端同步模式切换
- 同步状态展示
- 阈值配置器
- 保存与恢复默认值

适合后续接入真正的配置存储与云同步服务。

## 7. 实时数据与状态管理

### `useRealTimeData.js`

这个文件是项目中非常关键的“数据模拟中心”，提供多个自定义 Hook：

- `useVesselData(updateInterval)`
- `useEngineData(updateInterval)`
- `useNavigationData(updateInterval)`
- `useAlarmsData(updateInterval, language)`
- `useSystemStatus(updateInterval)`
- `useTrendData(...)`

这些 Hook 的作用是：
- 按固定时间间隔生成模拟数据
- 让页面看起来像真实系统一样持续刷新
- 支持不同页面共享同一数据模型

### 数据特点

#### 船舶数据
- 经纬度
- 航向 / 纵倾
- 风速 / 风向
- 吃水
- 航速
- 燃油消耗

#### 引擎数据
- RPM
- 功率
- 负载
- 燃油率
- 扭矩
- 排气温度
- 冷却温度
- 油压
- 涡轮转速
- 气缸温度数组

#### 告警数据
- 活动告警
- 历史告警
- 告警确认状态
- 中英文本地化

## 8. 语言国际化

`src/hooks/useLanguage.jsx` 提供完整的中英文文本表与上下文：
- `LanguageProvider`
- `useLanguage()`
- `translations`

页面内通过：
```js
const { t, language, setLanguage } = useLanguage();
```
来读取当前语言和切换语言。

### 国际化实现特点
- 主页、告警页、趋势页、配置页均使用同一套语言上下文
- 告警数据文本在钩子层也做了本地化处理
- UI 文本使用统一键名，便于后续扩展第三语言

## 9. 3D 模型系统

`src/YachtModel.jsx` 是 3D 视图的统一封装，包含：
- `YachtModel`
- `EngineModel`
- `EngineSystemsModel`
- `TrendEngineModel`

### 模型加载
- 游艇模型：`/costa_voyager/scene.gltf`
- 发动机模型：`/internal_combustion_engine/scene.gltf`

### 处理逻辑
- 使用 `useGLTF` 预加载模型
- 用 `Box3` 自动计算缩放和居中
- 使用 `OrbitControls` 控制相机
- 支持自动旋转
- 故障状态下高亮发光

### 注意事项
项目中依赖静态资源路径，部署时要确保：
- `public/` 中包含模型目录
- 图片、视频、GLTF 文件路径正确
- `homepage` 配置与部署子路径一致

## 10. 资源与静态文件约定

从代码中可以看出，项目使用了以下静态资源：
- `/image/logo.png`
- `/4880777-uhd_3840_2160_30fps.mp4`
- `/5024852-hd_1920_1080_24fps.mp4`
- `/3918100-hd_1920_1080_30fps.mp4`
- `/6028721-hd_1920_1080_25fps.mp4`
- `/internal_combustion_engine/scene.gltf`
- `/costa_voyager/scene.gltf`

这些文件一般应放在 `public/` 下，才能在构建后通过根路径访问。

## 11. 部署说明

### 当前构建命令
```bash
INLINE_RUNTIME_CHUNK=false CI=false react-scripts build
```

### 适配 CloudBase Hosting 的注意点
由于项目设置了：
- `homepage: "./"`
- 部署目录为 `./build`
- 部署路径为 `/eee`

这类配置通常适合静态托管在子目录下。

如果你遇到白屏，优先检查：
1. 资源路径是否正确
2. `homepage` 是否和部署子路径一致
3. 是否存在路由刷新 404
4. 浏览器控制台是否报错
5. 静态资源是否成功加载

## 12. 测试说明

当前 `src/App.test.js` 还是 CRA 默认模板测试：
- 测试内容仍是 `learn react link`
- 与当前业务页面不匹配

建议后续改成：
- 检查 `Dashboard` 是否渲染
- 检查导航按钮是否存在
- 检查中英文切换是否可用
- 检查告警页和配置页的关键文本是否渲染

## 13. 常见问题

### 13.1 部署后白屏
常见原因：
- `homepage` 与部署路径不一致
- 静态资源 404
- 3D 模型或视频资源找不到
- Leaflet 地图瓦片加载失败
- 某个页面运行时报错导致 React 挂掉

### 13.2 3D 模型不显示
检查：
- 模型是否放在 `public/` 目录
- 路径是否正确
- `useGLTF.preload` 是否能访问资源

### 13.3 地图显示异常
检查：
- `leaflet.css` 是否正确引入
- 地图容器是否初始化完成
- 是否调用了 `invalidateSize()`

### 13.4 告警数据不更新
检查：
- `useAlarmsData` 的刷新间隔
- 页面是否正确传入 `language`
- 是否有不必要的状态覆盖

## 14. 后续扩展建议

如果要继续完善，这个项目可以向以下方向扩展：
- 接入真实后端 API
- 将模拟数据替换成 WebSocket 实时流
- 将阈值配置持久化到云端
- 增加用户登录和权限控制
- 优化移动端适配
- 补充单元测试和页面级测试
- 抽离通用卡片、图表和仪表组件
- 增加告警确认 / 处理流程
- 给趋势页接入真实历史数据库

## 15. 总结

这个项目本质上是一个“船舶数字化监控大屏”原型，前端完成度较高，具备：
- 视觉呈现
- 交互操作
- 数据模拟
- 国际化
- 3D 可视化
- 地图展示
- 告警管理

适合作为船舶监控系统的前端展示层，也适合作为后续接入真实设备数据与海事系统的基础框架。
