# 开发进度文档

## 1. 项目名称

船舶监控大屏 `marine-dashboard`

## 2. 当前开发阶段

### 已完成内容
项目当前已经完成前端主框架搭建，具备较完整的可视化展示能力，包括：
- 主页面综合仪表盘
- 主机监控页
- 发动机系统页
- 航海地图页
- 告警页
- 趋势分析页
- 配置页
- 中英文切换
- 全屏控制
- 主题切换
- 3D 模型展示
- 实时模拟数据刷新

### 当前阶段定位
目前项目处于**前端可视化原型完成阶段**，核心 UI、交互和模拟数据链路已经打通，但数据来源仍以本地模拟为主，还未真正接入后端服务与数据库。

## 3. 已完成开发内容

### 3.1 前端基础框架
- 使用 React 构建项目结构
- 通过 `App` 统一挂载 `Dashboard`
- 页面采用模块化拆分方式
- 已接入 Tailwind、Framer Motion、Three.js、Leaflet 等依赖

### 3.2 页面功能已完成
- 综合驾驶台大屏首页
- 主机运行监控页面
- 发动机系统参数页面
- 告警管理页面
- 趋势分析页面
- 航海地图页面
- 系统配置页面

### 3.3 交互功能已完成
- 页面切换
- 3D 模型旋转控制
- 全屏显示
- 搜索弹窗
- 设置弹窗
- 账户弹窗
- 语言切换
- 主题切换
- 告警筛选与确认
- 地图点击联动
- 参数面板联动

### 3.4 数据模拟已完成
- 船舶实时位置模拟
- 引擎运行状态模拟
- 告警随机生成与确认状态模拟
- 航行数据模拟
- 趋势图数据模拟
- 系统健康状态模拟

## 4. 当前开发状态总结

目前前端已经可以独立运行并完成大部分展示逻辑，整体界面与交互已经接近真实监控系统形态。当前主要问题不在页面呈现，而在于**缺少真实后端接口和数据库支撑**，导致：
- 数据无法持久化
- 告警无法真正入库
- 配置无法云端同步
- 历史趋势无法读取真实历史数据
- 用户登录与权限暂未接入
- 设备状态无法从后端实时拉取

## 5. 后续开发目标

### 核心目标
后续开发的重点是实现**前后端数据库跑通**，即建立完整的数据闭环：

1. 前端页面能够调用后端接口
2. 后端能够读取和写入数据库
3. 数据库中的数据能够驱动前端展示
4. 前端操作能够反向写入数据库
5. 告警、配置、趋势、设备状态等数据具备持久化能力

## 6. 后续进度计划

### 阶段一：后端基础搭建
目标：先让前后端可以连通。

计划任务：
- 搭建后端项目基础结构
- 设计统一接口规范
- 配置数据库连接
- 完成环境变量管理
- 打通前端与后端的基础请求

预期结果：
- 前端可以访问后端服务
- 后端可以连通数据库
- 接口具备基本返回能力

### 阶段二：数据库模型设计
目标：建立可支撑页面数据的数据库结构。

计划任务：
- 设计船舶信息表
- 设计发动机状态表
- 设计告警表
- 设计趋势历史数据表
- 设计系统配置表
- 设计用户与权限表

预期结果：
- 业务数据有明确存储结构
- 前端所有核心页面都能找到对应的数据来源

### 阶段三：接口联调
目标：让页面从模拟数据切换到真实数据。

计划任务：
- 替换 `useRealTimeData` 的模拟逻辑
- 接入实时查询接口
- 接入告警确认接口
- 接入配置保存接口
- 接入趋势历史查询接口
- 接入地图和船舶状态接口

预期结果：
- 页面显示真实数据
- 告警状态变化可落库
- 配置修改可保存并同步

### 阶段四：数据库读写闭环
目标：让系统具备真正的业务运行能力。

计划任务：
- 完成新增、查询、更新、删除逻辑
- 建立数据同步机制
- 确保前端修改可写入数据库
- 确保数据库更新可实时反馈到前端
- 增加必要的数据校验与容错

预期结果：
- 前后端数据联动完整
- 核心页面不再依赖纯本地模拟
- 系统具备可持续迭代能力

## 7. 当前待解决问题

### 7.1 数据仍为本地模拟
目前页面中的实时数据来源主要是前端 Hook 模拟生成，需要替换成后端真实接口。

### 7.2 无持久化能力
告警确认、配置修改、历史趋势等操作还不能保存到数据库。

### 7.3 缺少统一后端协议
前端页面已经较完整，但还没有统一的后端接口文档和数据规范。

### 7.4 缺少数据库结构设计
当前还没有正式的表结构设计，后续需要统一建模。

## 8. 风险与注意事项

### 8.1 路由与资源路径
项目部署到子路径时要注意静态资源路径和 `homepage` 配置，否则会出现白屏或资源 404。

### 8.2 3D 与地图资源依赖
模型文件、视频文件和地图瓦片都依赖静态资源或外部服务，后续接后端时仍需保证资源可访问。

### 8.3 模拟数据到真实数据的切换成本
当前页面大量依赖模拟 Hook，后续切换真实接口时，建议先封装统一的数据访问层，避免大面积修改页面代码。

## 9. 下一步建议

为了尽快实现“前后端数据库跑通”，建议按以下顺序推进：

1. 先确定后端技术栈
2. 再设计数据库表结构
3. 定义前后端接口格式
4. 先接通基础查询接口
5. 再逐步替换模拟数据
6. 最后完善写入、同步与持久化

## 10. 当前里程碑状态

- 前端页面框架：已完成
- 页面交互逻辑：已完成
- 3D 可视化：已完成
- 地图展示：已完成
- 实时模拟数据：已完成
- 后端接口：待开始
- 数据库设计：待开始
- 前后端联调：待开始
- 数据持久化闭环：待开始

## 11. 结论

当前项目已经完成前端展示层的主体建设，下一阶段的重点不再是“补页面”，而是进入**系统集成阶段**。核心目标是把前端现有模拟数据与后端数据库打通，形成真实可用的船舶监控数据链路，为后续接入实际设备数据和业务逻辑打下基础。
